---
  # Result files
  RESULTS_DIR: "results"
  PAIR_RESULTS_DIR: "pair"

  # Data files
  DATA_DIR: "data"
  STATS_DIR: "data/stats"
  KV_DIR: "kvs"

  # Hyperparameters
  HPARAMS_DIR: "hparams"

  # Remote URLs
  REMOTE_ROOT_URL: "https://memit.baulab.info"
